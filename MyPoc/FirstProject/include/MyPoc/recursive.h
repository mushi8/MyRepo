#pragma once


/*Copy All Upper Case chars to a new array */
int copyUpperCase(const char* A, char* B);
/*Check if array of string contains a char recursively*/
bool existsInStrings(char* strings[], int size, char ch);
/*Check if a string contains a char recursively */
bool existsInString(char* string, char ch);
/*Claculate the num of ways to Climbe a ladder recursively*/
int numWays(int n);
